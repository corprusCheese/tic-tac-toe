{
  "name": "svelte-app",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd src/main/resources/front && npm install && npm run build",
    "frontend": "cd src/main/resources/front && npm start",
    "backend": "sbt run",
    "all": "sbt run && cd src/main/resources/front && npm run build"
  }
}
