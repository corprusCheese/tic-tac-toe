{
  "name": "svelte-app",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd src/main/resources/front && npm install && npm run build",
    "frontend": "cd src/main/resources/front && npm start",
    "backend": "sbt run",
    "all": "sbt run && cd src/main/resources/front && npm run build",
    "watch": "sbt run && cd src/main/resources/front && npm start"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^17.0.0",
    "@rollup/plugin-node-resolve": "^11.0.0",
    "rollup": "^2.3.4",
    "rollup-plugin-css-only": "^3.1.0",
    "rollup-plugin-livereload": "^2.0.0",
    "rollup-plugin-svelte": "^7.0.0",
    "rollup-plugin-terser": "^7.0.0",
    "svelte": "^3.0.0"
  },
  "dependencies": {
    "sirv-cli": "^1.0.0"
  },
  "engines": {
    "node": "14.x"
  }
}
